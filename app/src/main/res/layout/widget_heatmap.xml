<?xml version="1.0" encoding="utf-8"?>
<!-- Two full-size stacked ImageViews: background shape + heatmap cells bitmap -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/heatmap_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ic_launcher_background">

    <!-- Card background (shape + colour bitmap) -->
    <ImageView
        android:id="@+id/heatmap_widget_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY" />

    <!-- Heatmap cells — exact grid bitmap, fitCenter preserves square cells -->
    <ImageView
        android:id="@+id/heatmap_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"
        android:contentDescription="@string/heatmap_widget_name" />

    <TextView
        android:id="@+id/heatmap_status_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:text="@string/heatmap_widget_status_loading"
        android:textColor="@color/ic_launcher_foreground"
        android:textSize="12sp"
        android:fontFamily="sans-serif-medium"
        android:visibility="visible" />

</FrameLayout>
